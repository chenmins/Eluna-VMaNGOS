# Eluna-VMaNGOS 协作指南

本仓库是将 Eluna Lua 引擎整合进 VMaNGOS 的《魔兽世界》1.12 时代服务端模拟器。改动应优先保证客户端兼容性、玩法准确性、渐进式内容（patch progression）与数据一致性。

## 开始前

- 先阅读与任务直接相关的 `README.md`、现有实现、配置与 SQL；不要仅根据其他 WoW 核心或现代 TrinityCore 的行为推断本项目行为。
- 若根目录存在 `.codegraph/`，在用 `rg`、`find` 或大范围读取源码前，优先使用：
  - MCP：`codegraph_explore`
  - 命令行：`codegraph explore "<问题、符号或文件>"`
- 使用 CodeGraph 定位调用链、脚本注册点、配置项和数据库读写路径；需要精确上下文时再读取少量相关文件。
- 保持改动聚焦。不要在功能修复中顺带重格式化、重命名或重构无关代码。
- 工作区可能包含用户未提交的修改：先检查并保留它们，绝不使用 `git reset --hard`、`git checkout --` 等破坏性操作。

## 项目结构

- `src/game/`：游戏逻辑、对象、法术、地图、脚本管理等核心服务端代码。
- `src/shared/`、`src/framework/`：共享基础设施、数据库、网络和框架代码。
- `src/modules/Eluna/`：Eluna Lua 引擎集成与绑定。
- `src/scripts/`：C++ 服务端脚本；优先沿用同区域/副本的既有组织方式与注册模式。
- `lua_scripts/`：部署到服务端运行目录的 Eluna Lua 脚本。运行时由 `Eluna.ScriptPath` 指向该目录。
- `sql/`：认证、角色、世界、日志库结构及迁移；迁移位于 `sql/migrations/`。
- `conf/`：配置模板与示例。
- `dep/`：第三方依赖；除非任务明确涉及依赖，不要修改。
- `cmake/`、`CMakeLists.txt`：构建选项与平台配置。

## 玩法与内容原则

- VMaNGOS 的目标是尽可能准确复现原版行为，而不是加入默认开启的自定义玩法。
- 自定义行为应优先做成明确的配置项，并默认关闭；同时说明默认行为和兼容性影响。
- 内容优先使用数据库驱动或 Lua 脚本。只有数据库/脚本能力确实不足，才扩展 C++ 核心。
- 涉及任务、法术、AI、掉落、NPC、对象、刷怪、数值或客户端版本差异时，明确目标客户端/补丁阶段。
- 渐进式内容必须标明新增或变更所对应的补丁阶段；不得用后期版本数据覆盖早期版本行为。
- 修复“原版行为”前应先收集证据。优先级：官方补丁说明、同期蓝贴/存档、可信的历史数据或录像。将关键证据与推断写入提交说明、代码注释或任务说明。
- 不以私服实现或论坛传闻作为唯一行为依据。

## C++ 规范

- 遵循 `.clang-format` 与 `.editorconfig`：4 空格缩进、UTF-8、行尾换行；不要用 Tab。
- 延续邻近代码的命名、所有权、日志和错误处理模式；避免为了“现代化”而混入不一致的抽象。
- 改动并发、对象生命周期、网络封包、数据库异步操作、地图网格或法术流程时，先梳理调用链和线程/所有权边界。
- 不要忽略失败返回值；失败路径应保留对象和数据库状态的一致性。
- 新增 C++ 脚本时，确认其注册函数已加入对应脚本加载路径；仅新增源码文件并不代表脚本会运行。
- 仅在必要时修改 `dep/` 或公共接口，并说明兼容性和构建影响。

## Eluna 与 Lua 规范

- 先确认现有 Eluna 绑定是否已经支持所需行为；不要为方便而重复扩展相同 API。
- Lua 脚本应避免每 tick 的全表扫描、无边界计时器、未注销事件和全局污染。
- 事件回调应校验对象仍然有效、地图/实例上下文正确，且能安全处理玩家离线、怪物消失和重载。
- 脚本应在目标服务端运行目录的 `lua_scripts/` 下验证，并通过 `Eluna.ScriptPath` 配置加载。
- 涉及 Lua API 的新增或修改时，说明绑定入口、生命周期、线程假设及一个最小 Lua 使用示例。

## SQL 与数据迁移

- 不直接修改已发布迁移来“修正历史”；为新增变更创建符合现有命名与排序规则的迁移。
- 明确迁移属于 world、characters、logon 或 logs 数据库，并保持可重复执行或遵循项目既有幂等模式。
- 对数据删除、批量更新和版本/补丁字段尤其谨慎；先用精确条件查询确认影响范围。
- SQL 变更至少应验证：基准库导入、迁移合并/执行、无语法错误，及关键数据的预期结果。
- 仓库 CI 使用 MySQL 5.6 导入基础库并执行 `sql/migrations/merge.sh`；SQL 改动应与此流程兼容。

## 构建与验证

- 默认使用 out-of-source 构建目录，例如 `build/`；不要把生成物提交到仓库。
- CMake 关键选项包括 `USE_SCRIPTS`、`SCRIPT_LIB_ELUNA`、`USE_EXTRACTORS`、`ENABLE_MAILSENDER` 与 `SUPPORTED_CLIENT_BUILD`。沿用任务所需的现有配置，不要无理由改变默认选项。
- Linux CI 使用 GCC 与 Clang；Windows CI 使用 Visual Studio 2022 x64。修改跨平台代码或构建文件时，至少配置并构建受影响目标。
- C++ 改动至少执行受影响目标的构建；配置/安装布局变更还应验证安装结果。
- Lua 改动至少执行加载检查和目标事件的游戏内/集成验证。
- SQL 改动至少执行迁移验证；若本地无法完成完整数据库检查，明确说明未验证部分、原因和建议命令。
- 交付时报告实际执行的命令、结果，以及未执行的相关验证；不得把未运行的检查表述为已通过。

## 提交与交付

- 提交应是单一目的、可审阅的变更；不要混入构建产物、运行日志、数据库转储或本地配置密码。
- 说明行为变化、目标补丁/客户端、配置项、迁移顺序与验证证据。
- 若实现有已知限制或依赖外部数据，应在交付说明中明确写出。
